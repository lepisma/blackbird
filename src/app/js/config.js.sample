// Configurations
// --------------

var blackbird = blackbird || {};

(function(glob) {
    glob.config = {
        db: "./base/data.db",
        lastfm: {
            user: "",
            password: "",
            API_KEY: "",
            SECRET: ""
        }
    };
}(blackbird));
